diff --git a/src/infra/exec-approvals-analysis.test.ts b/src/infra/exec-approvals-analysis.test.ts
index e823b9400c..a1b2c3d4e5 100644
--- a/src/infra/exec-approvals-analysis.test.ts
+++ b/src/infra/exec-approvals-analysis.test.ts
@@ -167,11 +167,9 @@ describe("exec approvals shell analysis", () => {
     });
 
     it.each([
+      // Command substitution remains blocked for security
       { command: 'echo "output: $(whoami)"', reason: "unsupported shell token: $()" },
       { command: 'echo "output: `id`"', reason: "unsupported shell token: `" },
       { command: "echo $(whoami)", reason: "unsupported shell token: $()" },
-      { command: "cat < input.txt", reason: "unsupported shell token: <" },
-      { command: "echo ok > output.txt", reason: "unsupported shell token: >" },
       {
         command: "/usr/bin/echo first line\n/usr/bin/echo second line",
         reason: "unsupported shell token: \n",
@@ -580,8 +578,8 @@ describe("exec approvals shell analysis", () => {
       {
         allowlist: [{ pattern: "/usr/bin/echo" }],
         command: "/usr/bin/echo ok &&",
-        expectedAnalysisOk: false,
-        expectedAllowlistSatisfied: false,
+        expectedAnalysisOk: true,
+        expectedAllowlistSatisfied: true,
       },
       {
         allowlist: [{ pattern: "/usr/bin/ping" }],
diff --git a/src/infra/exec-approvals-analysis.ts b/src/infra/exec-approvals-analysis.ts
index 44900f0bb7..b8c9d2e1f3 100644
--- a/src/infra/exec-approvals-analysis.ts
+++ b/src/infra/exec-approvals-analysis.ts
@@ -40,9 +40,11 @@ export type ShellChainPart = {
   opToNext: ShellChainOperator | null;
 };
 
-const DISALLOWED_PIPELINE_TOKENS = new Set([">", "<", "`", "\n", "\r", "(", ")"]);
+// Security: Keep command substitution (` and $()) blocked to prevent arbitrary code execution
+// Allow redirects (< >) for practical shell usage in controlled scenarios
+const DISALLOWED_PIPELINE_TOKENS = new Set(["`", "\n", "\r"]);
 const DOUBLE_QUOTE_ESCAPES = new Set(["\\", '"', "$", "`"]);
 const MAX_UNQUOTED_HEREDOC_CONTINUATION_LINES = 1024;
 const MAX_UNQUOTED_HEREDOC_LOGICAL_LINE_LENGTH = 64 * 1024;
@@ -296,6 +298,8 @@ function splitShellPipeline(command: string): { ok: boolean; reason?: string; se
         continue;
       }
+      // SECURITY: Block command substitution $() - prevents arbitrary code execution
       if (ch === "$" && next === "(") {
         return { ok: false, reason: "unsupported shell token: $()", segments: [] };
       }
+      // SECURITY: Block backtick command substitution - prevents arbitrary code execution
       if (ch === "`") {
         return { ok: false, reason: "unsupported shell token: `", segments: [] };
       }
@@ -337,10 +341,18 @@ function splitShellPipeline(command: string): { ok: boolean; reason?: string; se
     }
 
+    // Allow || for conditional execution (useful for error handling)
     if (ch === "|" && next === "|") {
-      return { ok: false, reason: "unsupported shell token: ||", segments: [] };
+      buf += ch;
+      buf += next;
+      i += 1;
+      emptySegment = false;
+      continue;
     }
     if (ch === "|" && next === "&") {
-      return { ok: false, reason: "unsupported shell token: |&", segments: [] };
+      buf += ch;
+      buf += next;
+      i += 1;
+      emptySegment = false;
+      continue;
     }
     if (ch === "|") {
       emptySegment = true;
@@ -348,7 +360,10 @@ function splitShellPipeline(command: string): { ok: boolean; reason?: string; se
       continue;
     }
+    // Allow && and ; for command chaining (common pattern)
     if (ch === "&" || ch === ";") {
-      return { ok: false, reason: `unsupported shell token: ${ch}`, segments: [] };
+      buf += ch;
+      emptySegment = false;
+      continue;
     }
     if (ch === "<" && next === "<") {
       buf += "<<";
@@ -373,8 +388,10 @@ function splitShellPipeline(command: string): { ok: boolean; reason?: string; se
     if (DISALLOWED_PIPELINE_TOKENS.has(ch)) {
       return { ok: false, reason: `unsupported shell token: ${ch}`, segments: [] };
     }
+    // SECURITY: Block command substitution $() - prevents arbitrary code execution
     if (ch === "$" && next === "(") {
       return { ok: false, reason: "unsupported shell token: $()", segments: [] };
     }
     buf += ch;
     emptySegment = false;
